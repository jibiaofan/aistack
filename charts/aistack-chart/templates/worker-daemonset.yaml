{{- if .Values.worker.enabled }}
{{- $vendors := include "aistack.sortedVendors" . | fromJsonArray }}
{{- $base := include "aistack.workerBaseNodeSelectorJson" . | fromJson }}
{{- $pciIds := include "aistack.pciVendorIds" . | fromJson }}
{{- $dsBasename := printf "%s-worker" .Release.Name }}

{{- /*
Build the list of DaemonSets to render. Each entry carries:
  name          - logical name used in app labels and runtime tag
  dsName        - actual DaemonSet metadata.name
  runtime       - vendor runtime tag for per-vendor blocks (empty for CPU)
  nodeSelector  - effective nodeSelector (base merged with vendor label)
*/}}
{{- $workers := list }}

{{- /* CPU DaemonSet — always rendered first, owns the legacy name. */}}
{{- $cpuLabel := dict "feature.aistack.ai/acceleratable" "false" }}
{{- $cpuSel := mergeOverwrite (deepCopy $base) $cpuLabel }}
{{- $workers = append $workers (dict "name" "cpu" "dsName" $dsBasename "runtime" "" "nodeSelector" $cpuSel) }}

{{- /* GPU vendor DaemonSets — all get suffixed names. */}}
{{- range $vendor := $vendors }}
  {{- $pciId := index $pciIds $vendor }}
  {{- $vendorSel := dict }}
  {{- if $pciId }}
    {{- $pciLabel := printf "feature.node.kubernetes.io/pci-%s.present" $pciId }}
    {{- $vendorSel = dict $pciLabel "true" }}
  {{- end }}
  {{- $merged := mergeOverwrite (deepCopy $base) $vendorSel }}
  {{- $workers = append $workers (dict "name" $vendor "dsName" (printf "%s-%s" $dsBasename $vendor) "runtime" $vendor "nodeSelector" $merged) }}
{{- end }}

{{- $multiVendor := gt (len $workers) 1 }}

{{- range $idx, $w := $workers }}
{{- if $idx }}
---
{{- end }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: {{ $w.dsName }}
{{ include "chart_labels" $ | indent 4 }}
    {{- if $multiVendor }}
    app.kubernetes.io/component: worker
    aistack.io/runtime: {{ $w.name }}
    {{- end }}
  name: {{ $w.dsName }}
  namespace: {{ $.Release.Namespace }}
spec:
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: {{ $w.dsName }}
  template:
    metadata:
      labels:
        app: {{ $w.dsName }}
        {{- if $multiVendor }}
        app.kubernetes.io/component: worker
        aistack.io/runtime: {{ $w.name }}
        {{- end }}
    spec:
      # Disable Kubernetes service-link env injection. Kubernetes injects a
      # <SVCNAME>_PORT=tcp://... env var (name uppercased, '-' -> '_') for every
      # Service in the namespace; a AISTACK_-prefixed name would collide with
      # AIStack's AISTACK_-prefixed config defaults and crash-loop prerun
      # (e.g. a worker Service named aistack-worker -> AISTACK_WORKER_PORT).
      # AIStack sets every env it needs explicitly and resolves peers via DNS,
      # so it never relies on these link vars. KUBERNETES_SERVICE_HOST and
      # KUBERNETES_SERVICE_PORT are exempt from this flag and keep working.
      enableServiceLinks: false
      {{- with (include "image_pull_secrets" $ | trim) }}
      {{- nindent 6 . }}
      {{- end }}
      containers:
        - env:
            - name: AISTACK_SERVER_URL
              value: http://{{ $.Release.Name }}-server.{{ $.Release.Namespace }}.svc:{{ $.Values.server.apiPort }}
            - name: AISTACK_WORKER_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: AISTACK_RUNTIME_DEPLOY
              value: "Kubernetes"
            - name: AISTACK_RUNTIME_DEPLOY_MIRRORED_DEPLOYMENT
              value: "true"
            - name: AISTACK_RUNTIME_DEPLOY_MIRRORED_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: AISTACK_RUNTIME_KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: AISTACK_RUNTIME_KUBERNETES_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: AISTACK_WORKER_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            {{- if eq $w.runtime "hygon" }}
            - name: ROCM_PATH
              value: /opt/dtk
            - name: ROCM_SMI_LIB_PATH
              value: /opt/hyhal/lib
            {{- end }}
          envFrom:
            - secretRef:
                name: registration-token
                optional: false
            - configMapRef:
                name: worker-config
                optional: true
          image: {{ include "aistack.image" $ }}:{{ include "aistack.imageTag" $ }}
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          name: {{ $.Release.Name }}-worker
          resources: {}
          securityContext:
            allowPrivilegeEscalation: true
            capabilities: {}
            privileged: true
            readOnlyRootFilesystem: false
            runAsNonRoot: false
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - name: aistack-data-dir
              mountPath: /var/lib/aistack
            - name: cdi
              mountPath: /var/run/cdi
            - name: kubelet-device-plugins
              mountPath: /var/lib/kubelet/device-plugins
            {{- if eq $w.runtime "amd" }}
            - name: aistack-amd-driver
              mountPath: /opt/rocm
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "ascend" }}
            - name: aistack-ascend-driver
              mountPath: /usr/local/Ascend/driver
              readOnly: true
            - name: aistack-ascend-toolkit
              mountPath: /usr/local/Ascend/ascend-toolkit
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "hygon" }}
            - name: aistack-hygon-driver
              mountPath: /opt/hyhal
              readOnly: true
            - name: aistack-hygon-toolkit
              mountPath: /opt/dtk
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "metax" }}
            - name: aistack-metax-driver
              mountPath: /opt/mxdriver
              readOnly: true
            - name: aistack-metax-toolkit
              mountPath: /opt/maca
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "iluvatar" }}
            - name: aistack-iluvatar-toolkit
              mountPath: /usr/local/corex
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "cambricon" }}
            - name: aistack-cambricon-bin
              mountPath: /usr/bin/cnmon
            - name: aistack-cambricon-toolkit
              mountPath: /usr/local/neuware
              readOnly: true
            {{- end }}
            {{- if eq $w.runtime "thead" }}
            - name: aistack-thead-toolkit
              mountPath: /usr/local/PPU_SDK
              readOnly: true
            {{- end }}
            {{- with $.Values.worker.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: api
              containerPort: {{ $.Values.worker.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ $.Values.worker.metricsPort }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: api
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: /healthz
              port: api
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 5
            successThreshold: 1
      volumes:
        - name: aistack-data-dir
          hostPath:
            path: {{ $.Values.worker.dataDir }}
            type: DirectoryOrCreate
        - name: cdi
          hostPath:
            path: /var/run/cdi
            type: DirectoryOrCreate
        - name: kubelet-device-plugins
          hostPath:
            path: /var/lib/kubelet/device-plugins
            type: DirectoryOrCreate
        {{- if eq $w.runtime "amd" }}
        - name: aistack-amd-driver
          hostPath:
            path: /opt/rocm
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "ascend" }}
        - name: aistack-ascend-driver
          hostPath:
            path: /usr/local/Ascend/driver
            type: DirectoryOrCreate
        - name: aistack-ascend-toolkit
          hostPath:
            path: /usr/local/Ascend/ascend-toolkit
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "hygon" }}
        - name: aistack-hygon-driver
          hostPath:
            path: /opt/hyhal
            type: DirectoryOrCreate
        - name: aistack-hygon-toolkit
          hostPath:
            path: /opt/dtk
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "metax" }}
        - name: aistack-metax-driver
          hostPath:
            path: /opt/mxdriver
            type: DirectoryOrCreate
        - name: aistack-metax-toolkit
          hostPath:
            path: /opt/maca
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "iluvatar" }}
        - name: aistack-iluvatar-toolkit
          hostPath:
            path: /usr/local/corex
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "cambricon" }}
        - name: aistack-cambricon-bin
          hostPath:
            path: /usr/bin/cnmon
            type: FileOrCreate
        - name: aistack-cambricon-toolkit
          hostPath:
            path: /usr/local/neuware
            type: DirectoryOrCreate
        {{- end }}
        {{- if eq $w.runtime "thead" }}
        - name: aistack-thead-toolkit
          hostPath:
            path: /usr/local/PPU_SDK
            type: DirectoryOrCreate
        {{- end }}
        {{- with $.Values.worker.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if $multiVendor }}
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app.kubernetes.io/component: worker
              namespaceSelector: {}
      {{- end }}
      {{- if $w.nodeSelector }}
      nodeSelector:
        {{- toYaml $w.nodeSelector | nindent 8 }}
      {{- end }}
      hostNetwork: true
      hostIPC: true
      serviceAccountName: {{ $.Release.Name }}-worker
      dnsPolicy: ClusterFirstWithHostNet
      {{- if or (eq $w.runtime "nvidia") (eq $w.runtime "mthreads") }}
      runtimeClassName: {{ $w.runtime }}
      {{- end }}
  updateStrategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: RollingUpdate
{{- end }}
{{- end }}
